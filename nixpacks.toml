[phases.install]
cmds = ["npm install --production", "cd frontend && yarn install --ignore-engines"]

[phases.build]
cmds = ["cd frontend && CI=false yarn build"]

[start]
cmd = "npm start"

[variables]
NODE_VERSION = "22.13"
