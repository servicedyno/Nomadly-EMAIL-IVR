[phases.install]
cmds = ["npm install --production", "cd frontend && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "npm start"

[variables]
NODE_VERSION = "22"
